'use client'

import Layout from "@/containers/Layout";

export default function dashboardPage()
{
    return (
        <Layout>
            <h1 className="text-center">Olá bem vindo(a)!</h1>
        </Layout>
        
    )
}